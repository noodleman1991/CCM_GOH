# Homepage Slice H2 — Promote hard-coded sections to blocks + hero CTAs

> REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use `- [ ]`.

**Goal:** Make region-map + people-widget render as real homepage blocks (drop the feature-flag gates), set the hero CTAs to **Explore** / **Collaborate** (on staging), and give the hero a blob accent.

**Context (verified):** region-map + people-widget already have schemas (registered), componentMap entries, and are in the homepage `blocks[]` `of:` list (H1). The ONLY blockers are: (a) the `<Blocks>` renderer drops `region-map` when `!FEATURES.homepageMap` (`components/blocks/index.tsx:76`); (b) the legacy fixed-section path still flag-gates both (`homepage.tsx:242/257`) — dead now that blocks[] renders, removed here for cleanliness. Hero CTAs are content on the `hero-1` `links[]` field — set on the staging hero block, not a code change. The blob accent is a `hero-1.tsx` component addition (reuse the `ccmblob` keyframe).

## Global Constraints
- Green gate (`pnpm typecheck && pnpm test && pnpm build`) + dev/staging rendered validation (375 + RTL). Staging-only content edits. No AI attribution.

---

## Task 1: Drop the region-map renderer gate

**Files:** Modify `components/blocks/index.tsx`

- [ ] **Step 1:** Remove the gate at `components/blocks/index.tsx:74-77` (the `if (block._type === 'region-map' && !FEATURES.homepageMap) return false;` filter), so region-map renders wherever it's placed in blocks[]. If `FEATURES` becomes unused in the file, drop its import.
- [ ] **Step 2:** Run `pnpm typecheck` → no errors.
- [ ] **Step 3:** Commit: `feat(homepage): render region-map block unconditionally (drop homepageMap gate)`

## Task 2: Remove the dead flag-gated fixed sections

**Files:** Modify `components/pages/homepage.tsx`

- [ ] **Step 1:** In the legacy fixed-section fallback, remove the `{FEATURES.homepageMap && <RegionMapBlock .../>}` (line ~242) and `{FEATURES.homepagePeople && <PeopleWidget .../>}` (line ~257) blocks — these are unreachable once blocks[] renders, and region-map/people are real blocks now. Remove now-unused imports (`RegionMapBlock`, `PeopleWidget`, `FEATURES` if unused).
- [ ] **Step 2:** `pnpm typecheck && pnpm build` → compiles.
- [ ] **Step 3:** Commit: `refactor(homepage): drop dead flag-gated region-map/people fixed sections`

## Task 3: Hero CTAs → Explore / Collaborate (staging content)

**Files:** none (Sanity content on staging) — a small script.

- [ ] **Step 1:** Confirm the Explore + Collaborate routes. Explore = `/atlas`. Collaborate = the collab space route (verify: `app/[locale]/(main)/collaborate` exists → `/collaborate`).
- [ ] **Step 2:** Write `scripts/set-hero-ctas.mjs` (dry-run/--apply, refuse prod) that sets each homepage doc's first hero block (`blocks[0]` where `_type=="hero-1"`) `links` to two buttons: { title: "Explore", href/link: "/atlas", variant: default } and { title: "Collaborate", href/link: "/collaborate", variant: outline }. Use the EXACT `links[]` item shape `hero-1` expects (inspect `sanity/schemas/blocks/hero/hero-1.ts` `links` field + how `SanityButton` reads href — likely `{ _key, title, buttonVariant, link: {...} }` or `{ title, href }`). Match it verbatim.
- [ ] **Step 3:** Dry-run, then `--apply` on staging (development). Back up first.
- [ ] **Step 4:** Validate on dev: the hero shows Explore + Collaborate buttons linking to /atlas + /collaborate.
- [ ] **Step 5:** Commit: `feat(homepage): hero CTAs -> Explore + Collaborate (staging content + script)`

## Task 4: Hero blob accent

**Files:** Modify `components/blocks/hero/hero-1.tsx` (+ confirm `ccmblob` keyframe is globally available)

- [ ] **Step 1:** Add an absolutely-positioned organic blob accent behind/beside the hero content using the existing `ccmblob` animation (a `<div>` with `animate-[ccmblob_...]` or the project's blob utility; reduced-motion safe). Tint via `ccm-sky`/`ccm-sea`. Keep it decorative (`aria-hidden`).
- [ ] **Step 2:** `pnpm typecheck && pnpm build`.
- [ ] **Step 3:** Validate on dev (blob visible, doesn't overlap text, reduced-motion respected). Screenshot `docs/design/screenshots/homepage-hero-blob.png`.
- [ ] **Step 4:** Commit: `feat(homepage): hero blob accent`

---

## Self-Review
- region-map/people un-gated → Tasks 1+2 ✓; hero Explore/Collaborate → Task 3 ✓; blob → Task 4 ✓.
- No placeholders except the hero `links[]` shape, which Task 3 Step 2 resolves by inspecting the schema before writing the script (correct — don't guess the shape).
- Out of scope (unchanged from spec): blob/bubble MAP visual, hero feature carousel.
