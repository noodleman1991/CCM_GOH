# Phase 2 — Site-Wide Hardening & UX

Branch: `fix/sitewide-phase2` (off master after phase 1 merged at fedda1e6f).

## User-confirmed decisions
- Case-study **status drives visibility** (single source of truth). Migration approves imported (no-`submittedBy`) pending docs; user-submitted stay pending.
- **GDPR account deletion**: full hard delete (Prisma cascade + Clerk + private Sanity content). **Published case studies are RETAINED untouched**; user is told to email the CCM Hub team to change/remove them.
- **Drafts**: unify to server-side, cross-device; one-time migrate localStorage → server; delete localStorage path + dead zustand methods.
- **Email notifications**: bring back via Resend on case-study status change, server-side (webhook), i18n copy.
- Implementation by Fable (main agent, serial) while Fable subagents are down; exploration only may use other models.

## CONFIRMED SECURITY ISSUE (needs USER action)
The Sanity dataset `production_2` is **public** — anonymous queries succeed. 13 non-approved case studies (reviewNotes/submittedBy/author emails) are world-readable via the Data API. **User must set the dataset to private (or ACL) at sanity.io/manage.** App-layer mitigations done (see below) but the dataset ACL is the root fix.

## Status

### DONE (committed)
- [x] **Security**: revision-alert-provider reads via authenticated server route, not public client (`4c55263b6`).
- [x] **Status truth**: migration script `scripts/fix-imported-case-study-status.mjs` (dry-run default, `--apply` to write 9 imported docs) + Studio preview shows 🟢Live/🟡Pending/🔴 + read-only publishedAt (`51075f933`, `083f283a8`). **`--apply` NOT YET RUN — needs user go-ahead (writes prod CMS).**
- [x] **GDPR deletion**: synchronous `lib/account-deletion.ts` (Prisma cascade + delete private Sanity drafts/non-approved; retain published) + route + typed-DELETE UI + i18n publishedNote + 6 unit tests (`363f6327a`). Webhook backstop also erases private Sanity content (`eaed0cd5d`).
- [x] **UI**: logo no hover/transition (`f0712f26a`); cards show subtitle OR excerpt not both — grid-news/agenda/report + case-study-card (`992ccdfb4`).

### REMAINING
- [ ] **Drafts unification** (#15): form autosave → authenticated drafts API (debounced); GET endpoint on `app/api/case-studies/drafts/route.ts`; one-time localStorage→server migration; delete localStorage path in `components/forms/case-study-form.tsx` + dead `saveDraft/loadDraft` in `stores/case-study-store.ts`. NOTE: store's `loadDraft` is the OTHER public-client read — removing it closes that exposure too.
- [ ] **Email notifications** (#16): Resend send on case-study status→terminal via `app/api/webhooks/sanity/route.ts` (idempotent); `lib/email.ts` wrapper; i18n email copy; restore honest "you'll be notified" form copy.
- [ ] **UI visual** (#19 remainder, needs chromium/your eyes): sidebar 1px white vertical line + scroll blink + smooth behaviour (`components/ui/sidebar.tsx` rail `after:w-[2px]` line ~320, `transition-[left,right,width]`+`h-svh` line ~254; inset `m-2` gap); search smoothness (`components/search/*`); cookie banner persistence verify (logic looks sound; `hasConsented` init=true causes first-load flash).
- [ ] **Translations + testing guide** (#20): add missing en `onboarding.steps.workInfo.website`+`websitePlaceholder`; write gitignored `TESTING.md` (how to write/run tests + failure modes).

## Verification baseline
73 vitest tests pass, `pnpm typecheck` clean, phase-1 build succeeded (233/233) with `staticPageGenerationTimeout: 180`.
